bundle:
  name: quantlab-event-scanner

resources:
  jobs:
    phase1_manifest_trade_btc_probe:
      name: phase1_manifest_trade_btc_probe
      environments:
        - environment_key: phase1_probe_environment
          spec:
            environment_version: "2"
            dependencies:
              - pyyaml>=6.0
      tasks:
        - task_key: probe_latest_btc_trade
          environment_key: phase1_probe_environment
          spark_python_task:
            python_file: jobs/01_detect_events.py
            parameters:
              - --config
              - configs/dev.yaml
              - --sample-size
              - "20"

targets:
  dev:
    mode: development
    default: true
    workspace:
      root_path: /Workspace/Users/${workspace.current_user.userName}/.bundle/${bundle.name}/dev

  prod:
    mode: production
    workspace:
      root_path: /Workspace/Users/${workspace.current_user.userName}/.bundle/${bundle.name}/prod
